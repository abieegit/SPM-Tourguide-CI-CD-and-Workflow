# Contributing to TourGuide

1. Create a feature branch from `dev`: `git checkout -b feature-name dev`
2. Never commit directly to `main`.
3. Use clear, descriptive commit messages.
4. Open a Pull Request into `dev`, request a review before merging.
5. Resolve any merge conflicts locally before pushing.
